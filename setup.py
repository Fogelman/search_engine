from setuptools import setup, find_packages

setup(name='se',
      version='0.1',
      description='Basic Search Engine',
      license='MIT',
      packages=find_packages(),
      include_package_data=True,
      zip_safe=False)
